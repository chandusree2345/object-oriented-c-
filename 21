#include<iostream>
using namespace std;
int main()
{

    int num1,num2;
    cin>>num1>>num2;

    int gcd = 0, i = 1;
    do{
        if(num1%i==0 && num2%i==0)
        {
            gcd = i;
        }
        i++;
    }while(i<=num1 && i<=num2);

    cout<<"Gcd is: "<<gcd;

    return 0;
}
